import styles from './styles.module.css';

type Props = {
    value: string;
};

const ProductScript = (props: Props) => {
    return (
        <div className={styles.product_script}>

        </div>
    );
};

export default ProductScript;